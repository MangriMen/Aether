{
  "name": "@aether/monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build --continue",
    "clean": "turbo run clean && bun x rimraf .turbo && cargo clean",
    "client:build": "turbo run build --filter=@aether/client",
    "client:dev": "turbo run dev --filter=@aether/client",
    "lint": "turbo run lintt --continue",
    "prepare": "lefthook install",
    "ui:build": "turbo run build --filter=@aether/ui",
    "ui:dev": "turbo run dev --filter=@aether/ui"
  },
  "devDependencies": {
    "@typescript/native-preview": "^7.0.0-dev.20260517.1",
    "lefthook": "^2.1.6",
    "turbo": "latest",
    "typescript": "^6.0.3"
  },
  "packageManager": "bun@1.3.11",
  "engines": {
    "node": ">=24"
  }
}
