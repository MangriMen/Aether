# T-0.5 — Ужесточение `run_command`

| Поле | Значение |
| --- | --- |
| Статус | blocked |
| Шаг плана | Фаза 0, шаг 0.5 (A1) |
| Репозиторий | aether |
| Зависит от | — (Q7 решён) |
| Модель | Opus 5 — security-граница (RCE) |
| Ревью перед мержем | да |

## Что сделать
- Allowlist программ: разрешать только исполняемые файлы, путь которых хост сам и выдал
  (host-managed Java из `get_java`/`install_java`). Сравнение — по канонизированному пути.
- Убрать fallback на сырую строку в `plugin_command_to_host`: если `program` не резолвится
  в разрешённый путь — ошибка `PluginError::AccessViolation`, а не запуск как есть (F-13).
- Таймаут выполнения и лимит размера собранного stdout/stderr (обрезать + флаг «обрезано»).
- Изоляция окружения: `Command::env_clear()` + явный минимальный набор переменных;
  `current_dir` обязателен и обязан лежать внутри смонтированных плагину директорий.

### Решение по Q7 (2026-09-20)
Allowlist на этом проходе: запуск разрешён, **только если канонизированный путь программы
резолвится внутрь директории Java, управляемой ядром** (та, что выдают `get_java` /
`install_java`). Совпадение по имени файла (`java`, `java.exe`) основанием не является.
packwiz остаётся на jar-пути (Q1), поэтому этот путь обязан продолжать работать.

## Файлы
- `features/plugins/infra/extism/host_functions/features/core.rs` (`handle_run_command`)
- `features/plugins/infra/plugin_utils.rs` (`plugin_command_to_host`)
- `shared/serializable_command/domain/*` (`to_tokio_command`, возможно поле `env`)

## Как проверить
- `cargo test -p aether-core` + тесты: произвольный `program` (`cmd.exe`, `/bin/sh`) отклоняется;
  путь к host-managed Java проходит; вывод длиннее лимита обрезается.
- Ручной сценарий (обязателен): обновление packwiz-пака в инстансе всё ещё работает
  (пока jar-путь не удалён задачей T-2.4.3.D).

## Ловушки
- packwiz передаёт `program` как путь к Java, полученный из host-функции, а аргументы — в виде
  `#/cache/...` и `#/instances/...`; резолв `#`-путей ломать нельзя.
- На Windows `env_clear()` без `SystemRoot`/`PATH` ломает запуск JVM — оставить минимальный набор.

## Осталось
- **Ручной сценарий из «Как проверить» не выполнен** — обновление packwiz-пака в инстансе требует
  запуска GUI/Tauri и реального пака; в этой сессии окружения для этого нет. Из-за этого статус
  `blocked`, а не `done` (AGENT_INSTRUCTIONS §6). Всё остальное по карточке сделано и покрыто
  автотестами.
- Флаг «обрезано» доносится до плагина маркером в конце потока, а не полем `OutputDto`:
  добавление поля ломает уже собранные плагины (F-21). Поле добавить в T-1.4.

## Журнал
<!-- ГГГГ-ММ-ДД | модель | что сделано | коммит -->
2026-09-21 | Claude Opus 5 | Ужесточил `run_command`: (1) allowlist по Q7 — `resolve_allowed_program` резолвит `program` через `plugin_path_to_host`, канонизирует и требует, чтобы путь лежал внутри канонизированного `location_info.java_dir()`; запускается именно проверенный канонический путь; (2) убран fallback на сырую строку (F-13) — нерезолвящийся `program` даёт `PluginError::AccessViolation`; (3) `current_dir` теперь обязателен и проверяется на вхождение в смонтированные директории (`/cache`, `/instances`); (4) в `SerializableCommand` добавлено поле `env: Option<BTreeMap<..>>`: `None` = наследование (пользовательские pre-launch/post-exit команды не тронуты), `Some` = `env_clear()` + явный набор; плагинам выдаётся `plugin_utils::isolated_env()` (Windows: SystemRoot/windir/PATH/… — без них JVM не стартует); (5) `run_capped` — таймаут 15 мин с `kill_on_drop`, `stdin` = null, лимит 1 MiB на stdout и stderr с продолжением чтения после лимита (чтобы ребёнок не вис на полном пайпе) и маркером `[aether: output truncated]`. 20 новых тестов, `cargo test -p aether-core` — 273 passed, clippy чисто (остались только два пред-существующих ворнинга из F-15). Дополнительно прогнал разовую проверку на реальной установке (`%APPDATA%/aether.launcher`): настоящий путь `cache/java/zulu21…/bin/javaw.exe` проходит allowlist, `#/instances/Test` резолвится, env — ровно минимальный набор. Ручной сценарий обновления пака не выполнен (нет GUI) → `blocked`. Новые расхождения: F-21, F-22. | не закоммичено
