# T-0.4 — Вызов `manifest.validate()` при включении плагина

| Поле | Значение |
| --- | --- |
| Статус | blocked |
| Шаг плана | Фаза 0, шаг 0.4 (A6, A7) |
| Репозиторий | aether |
| Зависит от | — |
| Модель | Claude Sonnet 5 |
| Ревью перед мержем | нет |

## Что сделать
- Вызывать `PluginManifest::validate(&PLUGIN_API_VERSION)` в `EnablePluginUseCase::execute`
  до загрузки плагина; ошибку маппить в состояние `PluginState::Incompatible`/`Failed`
  с внятной причиной.
- В `ForceEnablePluginUseCase` обходить **только** проверку версии API, но **не**
  `RuntimeConfig::validate()` (запрет абсолютных `allowed_paths` должен действовать всегда).
- `api.features` пока не реализуем: если список непустой — писать предупреждение в лог
  (полноценная проверка — отдельная задача после T-1.4).

## Файлы
- `features/plugins/app/enable_plugin.rs`
- `features/plugins/app/force_enable_plugin.rs`
- `features/plugins/domain/models/plugin_manifest.rs` (если нужно разделить валидации)

## Как проверить
- `cargo test -p aether-core` + тест: манифест с абсолютным `allowed_paths` не включается
  ни обычным, ни force-путём.
- Ручной сценарий: packwiz (относительные пути, `apiVersion 0.2.0`) включается как раньше.

## Ловушки
- `validate` сейчас объединяет `runtime.validate()` и `api.validate()` — для force-пути их надо
  разделить, иначе force перестанет работать (F-12).
- Дефолтные `allowed_paths`, которые добавляет сам загрузчик (`/cache`, `/instances`),
  абсолютны на стороне хоста — валидация относится только к путям **из манифеста**.

## Проверка перед мержем (компенсация уровня модели)
Модель — Sonnet, поэтому обязательны:
1. Тест: force-enable по-прежнему обходит **только** проверку версии API, но не `RuntimeConfig::validate()`.
2. Ручная проверка: packwiz (`apiVersion 0.2.0`, относительные пути) включается как раньше.

## Осталось
— Ручной сценарий из раздела «Как проверить» (packwiz с `apiVersion 0.2.0` и относительными
  путями включается в реальном desktop-приложении как раньше) **не выполнен**: в этой сессии
  нет GUI/Tauri-окружения и нет доступа к файлам `../packwiz-plugin` (вне рабочей директории
  `aether`). Заменено автоматическим тестом `enable_succeeds_with_relative_paths_and_compatible_api`
  (относительные `allowed_paths` + удовлетворяемое требование к версии API, полный проход через
  `EnablePluginUseCase::execute` с мок-загрузчиком до состояния `Loaded`), который проверяет тот
  же путь кода, но не является заменой реального запуска. Владельцу нужно вручную подтвердить
  сценарий с реальным packwiz-плагином перед переводом задачи в `done`.

## Журнал
<!-- ГГГГ-ММ-ДД | модель | что сделано | коммит -->
2026-09-21 | Claude Sonnet 5 | Добавлен вызов `RuntimeConfig::validate()` в `EnablePluginUseCase::execute`
(безусловно, до загрузки) и в `ForceEnablePluginUseCase::execute` (тоже безусловно — обходится
только `check_compatibility`, т.е. проверка версии API); ошибка маппится в `PluginState::Incompatible`
+ `PluginError::Manifest`. Непустой `api.features` теперь логируется через `tracing::warn!` в обоих
use case (полноценная проверка отложена до после T-1.4, как и предписано карточкой). Добавлены 4
интеграционных теста в `packages/core/aether-core/src/features/plugins/tests/enable_plugin_test.rs`:
отказ по абсолютным `allowed_paths` в обычном и force-пути (в т.ч. при заведомо несовместимой
версии API — доказывает, что force обходит только версию), и успешный проход с относительными
путями в обоих use case. `cargo test -p aether-core` — 214/214 ok (включая `arch_test`);
`cargo clippy -p aether-core --tests -- -D warnings` чист для изменённых файлов (нашёл
пред-существующий F-15, не относящийся к T-0.4); `cargo fmt` применён. Ручной сценарий с реальным
packwiz не выполнен (см. «Осталось») — статус `blocked`, а не `done`. | 0e2df218
