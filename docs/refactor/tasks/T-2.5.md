# T-2.5 — Подключение executor к use case, Tauri и фронту

| Поле | Значение |
| --- | --- |
| Статус | not_started |
| Шаг плана | Фаза 2, шаги 2.3–2.4 (связывание) |
| Репозиторий | aether |
| Зависит от | T-2.4.1 |
| Модель | Claude Sonnet 5 |
| Ревью перед мержем | нет |

## Что сделать
Перевести `InstallContentUseCase` на `InstallPlanExecutor`: вместо ветвления
`Atomic`/`Modpack` — резолв плана у провайдера и его исполнение. Обновить DTO команды
`install_content` (`ContentInstallParamsDto`) и перегенерировать биндинги фронта.
UI-логику менять минимально: цель — сохранить существующие сценарии.

## Файлы
- `features/instance/app/use_cases/content_provider/install_content.rs`
- `apps/desktop/src/features/instance/infra/tauri/api/{commands.rs,dtos/content.rs}`
- `apps/frontend/src/shared/api/bindings/instance.ts` (генерируется), `apps/frontend/src/pages/content/*`

## Как проверить
- `cargo check` воркспейса, `bun run frontend:build`.
- Ручные сценарии: установка мода с Modrinth в инстанс; установка mrpack-модпака;
  повторная установка той же версии не создаёт дубликат.

## Ловушки
- Биндинги генерируются через specta/tauri-specta — не править `bindings/*.ts` руками.
- Старый путь `install_atomic` пока жив (удаляется в T-3.1) — не ломать провайдеров,
  ещё не мигрированных на момент этой задачи.

## Проверка перед мержем (компенсация уровня модели)
Модель — Sonnet, поэтому обязательны:
1. Биндинги фронта перегенерированы инструментом, а не правлены руками (`git diff` по `bindings/`).
2. Ручные сценарии: установка мода, установка `.mrpack`, повторная установка той же версии.
3. Проверить, что старый путь `install_atomic` не сломан (он ещё жив до T-3.1).

## Осталось
— нет

## Журнал
<!-- ГГГГ-ММ-ДД | модель | что сделано | коммит -->
